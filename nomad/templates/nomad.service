#!/sbin/openrc-run
  
name="nomad.service"

command={{ nomad_bin_dir }}/nomad agent -dev -config {{ nomad_conf_dir }}/{{ nomad_conf_file }}
command_user={ nomad_system_user }}
command_background="yes"

depend() {
        need net
}
